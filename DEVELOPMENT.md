# 项目开发规范

本文件适用于整个仓库，供开发者和代码助手共同遵循。先理解用户当前要求和已有实现，再做范围清楚、可以验证的修改；用户的明确要求优先。

## 项目组织

- 大厅与全部小游戏统一放在这一个 Git 仓库中。每款游戏放在 `games/<id>/`，不创建嵌套仓库、子模块或对原项目目录的依赖。
- 根目录 `index.html` 是大厅，`play.html` 是统一游玩页面；大厅代码放在 `hall/`，共用预览素材放在 `assets/previews/`。
- `hall/catalog.js` 是游戏登记的唯一来源。新增游戏应填写唯一 `id`、入口、类别、封面、完整截图、介绍、操作和可靠的 `readySelector`，由大厅生成列表和入口。
- 各游戏通过独立 iframe 运行，保留自己的主题、样式、快捷键和进度。大厅只负责展示、收藏、游玩记录和页面外壳，不接管游戏内部状态。
- 现有游戏沿用自身的模块划分：规则与状态逻辑同 DOM 渲染分开；不要为局部修改重写整款游戏或引入跨游戏全局变量。

## 技术与资源

- 默认使用原生 HTML、CSS、JavaScript，工具脚本要求 Node.js 18 或更新版本。保持现有游戏无需后端、数据库和在线 CDN 即可游玩。
- 新依赖应有实际必要性；新增需要编译的游戏时，接入根目录统一构建，不要求分别启动多个项目。
- 页面、脚本、样式、图片与字体使用相对资源路径，兼容域名根目录、GitHub Pages 项目子目录（如 `/ai-game/`）及本地直接打开页面。不要写死盘符、开发端口或站点根路径。
- iframe 入口必须来自游戏目录登记，不能把未经验证的 URL 参数直接用作加载地址。导入内容或用户文本应安全地呈现，避免直接拼入 HTML。
- 封面和预览优先使用真实游戏画面。引入外部素材时记录来源和许可，不用与玩法无关的图片代替场景。
- 文件使用 UTF-8，换行遵循 `.gitattributes`。修改带版本参数的运行资源后，同步更新 HTML 中对应的版本，避免旧缓存混用。

## 界面与交互

- 桌面优先在一屏展示主要玩法：场景、角色状态、当前任务、核心动作与常用物品操作。高度以 iframe 实际可用视口为准，不按物理屏幕高度估算。
- 优先整理布局和减少空白，不用全局缩放、过小字号或隐藏关键按钮硬塞一屏。较窄、较矮窗口允许必要的页面滚动，任何操作都必须可达。
- 长任务说明、背包、制作列表和附近人物等在各自面板内滚动；不要让长列表把主要操作推到整页底部。滚动区域提供清楚的标题或可访问名称，需要时可用键盘聚焦和滚动。
- 桌面与手机分别适配。手机可纵向排列，按钮保持可点击，避免横向溢出；不要将桌面的固定高度生搬到移动端。
- 保留各款游戏的视觉主题。按钮、输入框、弹窗和滚动条统一样式，同时保留语义、禁用状态、可见焦点与键盘操作。尊重减少动态效果和高对比度设置。
- 游戏详情弹窗的主按钮位于标题右侧，文案为“开始游戏”；游玩中的说明弹窗使用“返回游戏”。窄屏允许合理换行。
- Canvas 场景保持图形比例，调整画布大小或镜头后同步检查指针坐标换算、人物标签、地图边缘及战斗目标。每帧应覆盖或清理完整画布，避免尺寸变化产生残影。

## 存档与数据

- 保持存档键稳定：大厅 `youjian-library-v1`、逸剑风云决 `wandering-sword-v1`、余烬 `ember-wilderness-save-v1`。新游戏使用独立且明确的存档键。
- 大厅不读写游戏进度。打开游戏、返回大厅、调整布局或修改规则时，不自动清空存档或开始新周目。
- 变更存档结构应保留版本识别、旧版本兼容与必要的迁移备份；不要静默丢弃可恢复数据。导入前验证结构和值，读取失败时给出可理解的反馈。
- 重置、覆盖导入等会丢失进度的游戏内操作，应保留现有确认步骤和恢复能力。
- 浏览器验证使用独立上下文和测试存档，不连接、覆盖或清理用户的个人浏览器数据。不要提交真实存档、令牌、密钥或环境配置。

## 开发与验证

在仓库根目录运行：

| 命令 | 用途 |
| --- | --- |
| `npm start` | 启动源码预览，默认 `http://127.0.0.1:4177/` |
| `npm run build` | 生成可发布的静态文件到 `dist/` |
| `npm run preview` | 预览已构建的 `dist/`，默认端口同上 |
| `npm test` | 大厅与访问边界、两款游戏规则及主线策略验证 |
| `npm run test:browser` | 在真实浏览器中验证源码和交互 |
| `npm run test:browser:dist` | 在真实浏览器中验证构建产物 |

- 验证与改动范围匹配。纯文案或说明修改检查内容即可；布局修改使用真实浏览器检查尺寸与关键操作，规则或存档变更运行相关规则与迁移验证。不要为简单样式写只重复实现细节的测试。
- 桌面布局重点检查 1920×1080、1366×768、1280×720、1024×768，并检查较矮窗口的滚动回退；手机检查 390×844，必要时补充 320 像素宽度。
- 游戏界面检查应包含真实操作：例如探索移动、进入战斗与选择目标，或制作、使用物品与地图切换。仅截图正常不能替代交互验证。
- 修改游戏登记、加载入口、资源路径或构建逻辑后，执行构建并检查子目录访问；发布前以构建产物的浏览器验证结果为准。
- Playwright 和浏览器准备方式见 `README.md`。浏览器结果放在被忽略的 `test-results/`，游戏专项测试沿用各自目录中的入口。
- 若验证受环境限制，交付时说明已验证范围和剩余限制，不将未运行的检查写成通过。

## 构建与提交

- `scripts/site-files.cjs` 负责收集公开运行文件。开发规则、测试、脚本、依赖目录、缓存、存档和敏感配置不得进入发布产物；增加资源类型时同步检查收集边界。
- `dist/`、`node_modules/`、`test-results/` 等生成内容遵循 `.gitignore`，不提交到源码仓库。
- 修改以本仓库内文件为准，保留已有未提交改动。不要为了修界面改写原来的独立项目、回退他人的工作或顺手删除无关文件。
- 只有用户要求时才执行暂存、提交或推送；用户只要提交说明时，提供可复制的说明。提交信息写清用户可见变化和相关验证，不混入无关改动。
- 新增游戏或改变启动、构建、存档方式时，同步更新 `README.md` 和必要的游戏说明。
